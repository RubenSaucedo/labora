# /build-resume <persona> <job-slug> [--style N]

Launch the `resume-build` agent (`agents/resume-build.agent.md`). It runs the
hash-aware assurance pipeline end to end and finishes with
`applications/<job-slug>/release.json`.
